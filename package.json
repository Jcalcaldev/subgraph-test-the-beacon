{
  "name": "the-beacon-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ the-beacon-test",
    "create-local": "graph create --node http://localhost:8020/ the-beacon-test",
    "remove-local": "graph remove --node http://localhost:8020/ the-beacon-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 the-beacon-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
